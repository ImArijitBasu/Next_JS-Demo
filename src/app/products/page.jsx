import React from 'react';
import Button from '../components/Button';

const page = () => {
    return (
        <div>
            product page
            <Button/>
        </div>
    );
};

export default page;